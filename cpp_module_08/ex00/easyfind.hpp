#ifndef EASYFIND_HPP
#define EASYFIND_HPP

#include <iostream>
#include <string>
#include <algorithm>
#include <vector>

template<typename T>
int easyfind(T & a, int & n) {
	int tab[] = {1,2,3,4,5};
	(void)tab;
	(void)n;
	(void)a;
	//p = std::find (myints, myints+4, 30);
	return -1; //case of error

}

#endif